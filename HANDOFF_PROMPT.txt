Build a VS Code extension in c:\\btr\\camelot\\extensibility\\TestExplorer. Goal: replicate VS Code C# Dev Kit test explorer UX (Testing Explorer tree, run/debug icons next to methods, results). Support xUnit v3 (v2 nice-to-have). Use a TypeScript extension using VS Code Testing API + a .NET worker using Roslyn/MSBuildWorkspace for discovery (accurate uri/range). Run and debug via `dotnet test` (VSTest) using `--filter` and TRX parsing. Depend on ms-dotnettools.csharp for debugging. See PLAN.md for details.
